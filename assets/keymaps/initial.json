// VibeDev keymap
//
// For information on binding keys, see the upstream Zed reference at
// https://zed.dev/docs/key-bindings — VibeDev shares the editor
// keybinding schema.
//
// To see the default key bindings run `zed: open default keymap` from
// the command palette. The command-palette namespace stays `zed:`
// because it matches the underlying editor command identifiers.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    },
  },
]
