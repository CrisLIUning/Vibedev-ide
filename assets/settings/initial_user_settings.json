// VibeDev settings
//
// For information on how to configure VibeDev, see the documentation
// at https://aitoken.bigopen.cn/docs/configuring-vibedev (forthcoming).
// VibeDev is built on the Zed editor; upstream Zed's configuration
// reference at https://zed.dev/docs/configuring-zed also applies to
// most keys here.
//
// To see all default settings without changing your custom settings,
// run `zed: open default settings` from the command palette
// (cmd-shift-p / ctrl-shift-p). The command palette namespace stays
// `zed:` because it matches the underlying editor command identifiers.
{
  "ui_font_size": 16,
  "buffer_font_size": 15,
  "theme": {
    "mode": "system",
    "light": "One Light",
    "dark": "One Dark",
  },
}
